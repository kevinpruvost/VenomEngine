build --enable_platform_specific_config

# Set C++20 standard for MSVC
build:windows --cxxopt="/std:c++20"

# Set C++20 standard for Clang/GCC
build:linux --cxxopt="-std=c++20"
build:macos --cxxopt="-std=c++20"